import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Cookie Policy",
	description: "How OpenChats uses cookies and similar technologies."
};

export default function CookiePolicyPage() {
	return <div className="section-padding">Cookie Policy</div>
}
